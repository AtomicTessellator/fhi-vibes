To run this example use the command:
hilde fireworks add_wf -w workflow.in  

and then run the FireWorks workflow with 
hilde fireworks ralaunch rapidfire

(assuming no other active FireWorks on the LaunchPad)
